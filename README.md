# sem4

 - Run project: 
    1. Open terminal in **build** folder
    2.  run `yarn start`

 - Seed database:
    1. Open terminal in **build** folder
    2.  run `yarn seed`

 - Build project:
  	1. Open terminal in root folder
    1.  run `yarn build`